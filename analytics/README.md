# Analytics Tools

Analytics dashboard and utilities for HazeBot Admin Panel usage tracking.

## 📊 Dashboard

Interactive HTML dashboard with real-time analytics visualization.

**Quick Start:**
```bash
python view_analytics.py
```

Opens dashboard at `http://localhost:8080` with auto-browser launch.

### Features

- **User Metrics** - Total users, active users (7d), sessions
- **Session Stats** - Average duration, total actions
- **Charts**:
  - 📈 User Growth (daily active users)
  - 📱 Device Distribution (platform breakdown)
  - 🔥 Top Features (most used endpoints)
  - ⏰ Peak Usage Times (24h activity)
- **Recent Sessions** - Last 20 sessions with details
- **Filters** - Time range (7d/30d/90d/all)
- **Auto-Refresh** - Optional 30s updates

### Usage

```bash
# Default (port 8080, auto-open browser)
python view_analytics.py

# Custom port
python view_analytics.py --port 9000

# No auto-browser
python view_analytics.py --no-browser
```

## 📁 Files

- **`analytics_dashboard.html`** - Standalone dashboard (Chart.js)
- **`view_analytics.py`** - Local HTTP server for dashboard
- **`test_api_endpoints.py`** - API testing script (optional)

## 🔗 Data Source

Dashboard reads from:
- `../TestData/app_analytics.json` (development)
- `../Data/app_analytics.json` (production)

Generated by the analytics system in HazeBot API (`api/analytics.py`).

## 🎨 Design

- **Dark Mode** - Slate/navy theme (#0f172a, #1e293b)
- **Responsive** - Works on desktop, tablet, mobile
- **Compact Layout** - Maximum info density
- **Interactive** - Hover effects, clickable legends

## 📝 Notes

- Dashboard is **read-only** - no modifications to data
- Works offline once loaded (local file access)
- Can be hosted on any static web server
- No external dependencies except Chart.js (CDN)
